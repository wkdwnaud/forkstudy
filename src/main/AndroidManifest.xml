<?xml version="1.1" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.CodeWave.lotto">

    <application
        android:allowBackup="true"
        android:icon="@drawable/moneybackground_white"
        android:label="@string/app_name"
        android:roundIcon="@drawable/moneybackground_white"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ResultActivity"
            android:theme="@style/AppTheme.Result" />
        <activity
            android:name=".NameActivity"
            android:theme="@style/AppTheme.Name" />
        <activity
            android:name=".ConstellationActivity"
            android:theme="@style/AppTheme.Constellation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>